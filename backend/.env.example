# Server
NODE_ENV=development
PORT=3000

# Database — matches backend/docker-compose.yml (docker compose up -d)
DATABASE_URL=postgres://postgres:postgres@localhost:5433/metria

# Auth
JWT_SECRET=metria-dev-secret-change-me-in-production-0123456789
JWT_EXPIRES_IN_SECONDS=604800
